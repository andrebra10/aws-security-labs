# Copy to .env for local testing (e.g. with SQLite) outside of Docker/AWS.

APP_MODE=development
SECRET_KEY=local-testing-secret-key
DATABASE_URL=sqlite:///./ppk_local.db
S3_BUCKET_NAME=
AWS_REGION=eu-west-1

# Only used when APP_MODE=development
DEV_USERNAME=pepe
DEV_PASSWORD=Summer2025!
